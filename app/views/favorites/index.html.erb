<p id="notice"><%= notice %></p>
<h1>Listing Favorites</h1>
<table class="table table-hover">
  <thead>
    <tr>
      <th>Nome do filme</th>
      <th>Email do usuário</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @favorites.each do |favorite| %>
      <tr>
        <td><%= favorite.movie.name %></td>
        <td><%= favorite.user.email %></td>
        <td><%= link_to "Show", favorite, class: "btn btn-primary" %></td>
        <td><%= link_to "Edit", edit_favorite_path(favorite), class: "btn btn-primary" %></td>
        <td><%= link_to "Destroy", favorite, method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-danger " %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>
<%= link_to "New Favorite", new_favorite_path %>
